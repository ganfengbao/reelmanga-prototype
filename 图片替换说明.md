# 图片替换说明（真实图片模板）

所有图片统一放在 `images/` 目录下。你只需用**同名、同后缀**的真实图片放入对应目录（当前目录为空），网页会自动生效，无需改任何代码。**没有图片时，对应位置会显示为空白**（不会出现破图图标）。

## 目录结构

```
原型图/
├── index.html
└── images/
    ├── characters/          # 角色图（透明底 PNG）
    │   ├── sunian.png       # 女主 苏念
    │   ├── wangye.png       # 男主 墨尘
    │   ├── fanpai.png       # 反派 沈逸
    │   └── qunxiang.png     # 群像/配角
    └── scenes/              # 场景图（9:16 竖版）
        ├── bed.png          # 寝殿
        ├── hall.png         # 大堂
        ├── garden.png       # 庭院
        └── city.png         # 皇城
```

## 替换对照表

### 角色图（`images/characters/`）

| 文件名 | 角色 | 建议尺寸 | 建议格式 |
|--------|------|----------|----------|
| `sunian.png` | 苏念 · 女主 | 720×1080 左右 | **透明底 PNG**（全身或半身，人物居中靠下） |
| `wangye.png` | 墨尘 · 男主 | 720×1080 左右 | 透明底 PNG |
| `fanpai.png` | 沈逸 · 反派 | 720×1080 左右 | 透明底 PNG |
| `qunxiang.png` | 群像 · 配角 | 720×1080 左右 | 透明底 PNG |

> ⚠️ 角色图请用**透明背景**（PNG）。网页会把角色叠加到场景图上，底部对齐、水平居中，宽度固定为画面的 72%。
> 若没有透明图，用白底/纯色底也可以，但会盖住场景，建议扣图后使用。

### 场景图（`images/scenes/`）

| 文件名 | 场景 | 建议尺寸 | 建议格式 |
|--------|------|----------|----------|
| `bed.png` | 寝殿 | 720×1280 | JPG/PNG（9:16 竖版） |
| `hall.png` | 大堂 | 720×1280 | JPG/PNG |
| `garden.png` | 庭院 | 720×1280 | JPG/PNG |
| `city.png` | 皇城 | 720×1280 | JPG/PNG |

> 场景图建议 **9:16 竖版**，会以 `object-fit: cover` 铺满整帧。

## 图片都用在哪些地方

- **创作台 · 分镜台**：每一镜 = 场景图（背景）+ 角色图（前景）+ 台词字幕。10 个镜头会自动复用这 4 个角色 + 4 个场景组合。
- **创作台 · 人物场景生成**（GPT Image 2 阶段）：直接展示角色图与场景图。
- **播放器**：播放成片时逐镜切换场景 + 角色 + 字幕。
- **首页悬浮预览卡、作品库缩略图、项目中心封面**：由「场景图 + 角色图」自动合成。

## 如何替换

1. 把真实图片重命名为上表的**文件名**（例如女主的图命名为 `sunian.png`）。
2. 覆盖到对应目录（`images/characters/sunian.png`）。
3. 刷新网页即可看到；若发布到 GitHub Pages，把图片一起提交推送即可。

## 想用更多角色 / 场景怎么办

在 `index.html` 的 `<script>` 里找到下面两处，照着加即可：

```js
// 角色：新增一个 key，并在 images/characters/ 放同名 png
const CHARACTERS = { sunian:{...}, wangye:{...}, /* 新增 mychar:{name:"新角色", silo:"girl", accent:"#fff"} */ };

// 场景：新增一个 key，并在 images/scenes/ 放同名 png
const SCENES = { "寝殿":{file:"bed"}, /* 新增 "新场景":{file:"news"} */ };
```

并把新角色/场景的 `key` 填进分镜数据 `SHOTS` 里即可。
